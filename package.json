{
  "name": "dispatchrr-backend",
  "version": "0.1.0",
  "private": true,
  "description": "POC dispatch backend API for jobs, drivers, assignments, and status updates.",
  "type": "module",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
